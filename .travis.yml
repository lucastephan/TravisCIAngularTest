language: node_js
node_js:
  - ""
dist: trusty
sudo: required

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

script:
  - ng lint
  - ng build --prod --base-href https://lucastephan.github.io/TravisCIAngularTest/
